machine:
  node:
    version: "0.12.7"
test:
  override:
    - npm run ci
deployment:
  release:
    tag: /^v[0-9]+(\.[0-9]+)*$/
    owner: rsrichardstevens
    commands:
      - echo -e "$NPM_USER\n$NPM_PASS\n$NPM_EMAIL" | npm login
      - npm publish
      - npm logout